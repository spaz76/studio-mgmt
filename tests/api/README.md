# Mobile API Tests

Placeholder directory for future REST/GraphQL API endpoint tests.

Tests added here will exercise the same service layer (`src/services/`) that
the Next.js server actions use, validating that the business logic works
correctly when called from HTTP API handlers.

Run with: `npm run test:api`
